#include "texture.h"


int point_dans_polygone(Points p,polygone poly);
int polygone_dans_polygone(polygone poly0,polygone poly1);
Points sur_un_point(Points p_sourie,liste l_poly);
Points sur_un_point_poly(Points p_sourie,polygone p);
